function personInfo(firstName, lastName, age) {
    let person = {
        firstName,
        lastName,
        age,
    };

    return person;
}

// Example usage:
let person1 = personInfo("Peter", "Pan", 20)
console.log(person1);

